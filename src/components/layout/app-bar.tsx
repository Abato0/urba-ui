import { AppBar } from '@material-ui/core';
import React from 'react';


const NavBar: React.FC=({children})=>{
    return (<AppBar color='primary' position='fixed'>

        das
    </AppBar>)
}

export default NavBar